# WORKAROUND: OAuth scope limitations prevent workflow file updates
# This file temporarily contains lean CI requirements to fix PyAudio build failures
# Original production requirements backed up as requirements-backup.txt
#
# Lean CI-focused requirements - excludes heavy ML/CUDA dependencies
# Core FastAPI dependencies
fastapi>=0.115
uvicorn>=0.30
pydantic[email]>=2.7
pydantic-settings>=2.0.0
python-dotenv>=1.0

# Database and storage (lightweight)
sqlmodel>=0.0.16
python-dateutil>=2.8.0

# Testing dependencies
pytest>=7.4
pytest-asyncio>=0.21.0
pytest-mock>=3.10.0
httpx>=0.24.0

# Basic Azure dependencies (no heavy ML)
azure-identity>=1.17
azure-core>=1.29.0
msal>=1.30.0
aiohttp>=3.9.0
cachetools>=5.3.0

# Document processing (lightweight)
pypdf>=4.2.0
python-docx>=1.1.2
aiofiles>=23.1.0

# Utilities
psutil>=5.9.0
requests>=2.31

# Skip heavy dependencies for CI:
# - sentence-transformers (200MB+)
# - torch/CUDA dependencies (3GB+)
# - azure-search-documents (when ML features disabled)
# - SpeechRecognition/pyaudio (platform-specific)
# - python-pptx (can be mocked in tests)
# - PyMuPDF (large binary dependencies)
# - azure-storage-blob (can be mocked)
# - azure-servicebus (can be mocked)
# - azure-cosmos (can be mocked)
# - playwright (large browser dependencies)